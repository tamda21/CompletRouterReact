/*import React from 'react';

let Rainbow =(wrapper)=>{
  let colors = ['red','blue','orange','yellow', 'green'];
  let randColors = Math.floor(Math.random()*(colors.length));
  let className =randColors+'-text';
  return (props)=>{
    return (
      <div className={className}>
         <wrapper {...props}/>
      </div>
      );
  }
}
export default Rainbow*/